{
  "name": "saguaro-labs-quote-tool",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vercel dev",
    "build": "node build.js",
    "test": "jest",
    "test:coverage": "jest --coverage",
    "lint": "eslint api/ frontend/ tests/ --no-error-on-unmatched-pattern",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "formidable": "^3.5.1",
    "googleapis": "^144.0.0",
    "nodemailer": "^6.9.14",
    "stripe": "^16.0.0",
    "uuid": "^10.0.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "esbuild": "^0.23.0",
    "eslint": "^9.0.0",
    "jest": "^29.0.0",
    "vercel": "^37.0.0"
  }
}
